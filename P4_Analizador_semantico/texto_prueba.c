//Un programa C para probar el analizador

principal(){
    entero var1 = 2, var2, numero2 = 10;
    real numero;
    bool bit;
    
    elevarCuadrado(entero cosa){
        cosa = cosa * var1;
    }

    procedimiento(entero numero1, entero numero22){
        real nada;
        entero otro, var2;
        //entero numero1; //esto da error semantico: redefinicion de variable
        si (1==1)entonces {

        }
        numero1 = (numero1 * -numero2) % 5;
    }
    para contador := 0 sube hasta 1 haz 
        var1 = var1 + 2;

    si (!(bit == falso)) entonces
        var1 = 2;
    sino{
        var1 = var1 * 2;
        numero ++;
        
    }   

    elevarCuadrado(var1);   

    numero = (0.2 + (0.2 * 2.3))/10.7;

    real lectura;

    si ((lectura == 2.0)&&(var1 != 4)) entonces
        leer(lectura);

    lista de bool lista1 = [bit];
    lista de real lista2 = [2.3,5.1], lista3 = [1.0];
    lista de real lista4;

    lista4 = [2.3,5.6];
    lista2 = lista2 ** lista3;
    lista2 = lista2 -- 5;
    
    numero = lista3 @ 7;

    lista2 = lista2 ++ 2.3 @ 5;
    lista2 = lista2 ++ (var1++) @ 3;
    lista4 = lista2 ++ (lista3 @ 2) @ 4;

    $lista2;
    lista2 >>;
}
